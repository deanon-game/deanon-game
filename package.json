{
  "name": "pygmalemon",
  "version": "0.0.1",
  "private": true,
  "main": "back/app.js",
  "scripts": {
    "heroku-prebuild": "cd back/ && npm i && cd ../front/ && npm i",
    "build": "ls && cd front/ && npx vue-cli-service build",
    "start": "node back/app.js"
  },
  "cacheDirectories": ["front/node_modules", "back/node_modules"],
  "engines": {
    "node": "10.x"
  }
}
